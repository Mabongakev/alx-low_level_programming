#include "main.h"
/**
 * _abs -  a function that computes the absolute value of an integer
 * @k: function parameter
 * Return: -k or k
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
